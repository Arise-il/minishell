minishell/
├── Makefile
├── README.md
├── minishell.h            # Global includes and struct definitions
├── libft/                 # Your own libft (if needed)
│   └── ...
├── src/
│   ├── main.c             # Entry point and main shell loop
│
│   ├── parser/            # YOUR PART (PARSING)
│   │   ├── parser.c           # Main function that starts parsing
│   │   ├── tokenizer.c        # Turns input string into tokens
│   │   ├── expander.c         # Handles $VARIABLE expansions
│   │   ├── syntax_checker.c   # Checks for syntax errors
│   │   ├── cmd_builder.c      # Converts tokens into t_cmd structs
│   │   ├── heredoc.c          # Handles heredoc logic (`<<`)
│   │   └── parser_utils.c     # Helper functions
│
│   ├── executor/          # YOUR PARTNER'S PART (EXECUTION)
│   │   ├── executor.c         # Executes parsed commands
│   │   ├── builtins.c         # Builtin command implementations
│   │   ├── redirections.c     # Handles >, <, >>, <<
│   │   ├── pipes.c            # Manages pipe setup
│   │   └── exec_utils.c       # Helper functions
│
│   ├── env/               # Optional: env var handling
│   │   ├── env_list.c         # Convert envp to linked list
│   │   ├── env_utils.c        # Add, update, remove env vars
│   │   └── export_sort.c      # Sort for `export` display
│
│   ├── signals/           # Signal handling
│   │   ├── signals.c          # SIGINT, SIGQUIT, heredoc signals
│   │   └── signals_utils.c    # Bonus utils for signals
│
│   └── utils/             # Shared utility functions
│       ├── str_utils.c        # Custom ft_split, strjoin, etc.
│       ├── mem_utils.c        # Freeing, memory cleanup
│       └── error.c            # Error printing and exit helpers
│
└── includes/
    ├── minishell.h        # Main include file with all project-wide headers
    ├── parser.h           # Parsing-related structs and function prototypes
    ├── executor.h         # Execution-related structs and prototypes
    ├── env.h              # Environment variable handling
    └── signals.h          # Signal-related includes
